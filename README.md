# Chowdeck

React single-page application powered by Vite and styled with Tailwind CSS v4.

## Getting Started

Install dependencies and start the Vite development server:

```bash
npm install
npm run dev
```

Create a production build with:

```bash
npm run build
```
